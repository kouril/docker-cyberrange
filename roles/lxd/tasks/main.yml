---
- name: Install the LXD package
  apt:
    name: lxd
    state: present
    update_cache: yes
  register: lxd

- name: Initialize LXD
  vars:
    lxd_conf: "{{ lookup('template', 'configuration.yml') }}"
  shell:
    cmd: lxd init --preseed
    stdin: "{{ lxd_conf }}"
  when: lxd.changed
